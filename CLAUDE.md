# HR 考勤智能预警系统

## 项目定位
面向 HR 的考勤异常智能预警平台，核心能力：自动识别考勤异常 → 分级预警 → 推送通知 → 数据看板。

## 技术栈
- 待定（讨论后确定）

## 目录结构
```
hr-attendance-alert/
├── src/
│   ├── api/          # 对外接口层
│   ├── core/         # 核心业务逻辑
│   ├── models/       # 数据模型定义
│   ├── services/     # 业务服务层
│   └── utils/        # 工具函数
├── config/           # 配置文件
├── docs/             # 项目文档
├── tests/            # 测试
└── CLAUDE.md         # 本文件
```

## 开发规范
- 中文注释，英文代码/变量/文件名
- 改完必须跑测试验证，不要只改不验
- 大改动前先 PlanMode 出方案
- 密钥/token 不进代码、不进 commit
- 遵从用户 CLAUDE.md 中的红线规则

## 验证命令
（项目搭建后补充）
